# Working notes — local only, not part of the public repo
TASKS.md
.claude/
stories/*/trip.md

# Build tooling deps (Field Notes) — not served, not tracked
node_modules/

# macOS
.DS_Store

# Local tooling cache + parked files + analytics exports — never tracked
.wrangler/
_to_delete/
plausible-import/
